from imports import *



with Window( title="Scrollable", center=True):
    with Container(center=True, width_fill=True, height_fill=True) as cont_id:
        
        add_text(
            content="This is Some Text",
        )


start_session()
